<?php
// Auto-generated by wp-light for local development. Safe to edit further,
// but the DB_* constants are managed by the app — changing them without
// updating the wp-light registry will break `wplight export`.

define( 'DB_NAME', '{{DB_NAME}}' );
define( 'DB_USER', '{{DB_USER}}' );
define( 'DB_PASSWORD', '{{DB_PASSWORD}}' );
define( 'DB_HOST', '127.0.0.1:{{MYSQL_PORT}}' );
define( 'DB_CHARSET', 'utf8mb4' );
define( 'DB_COLLATE', '' );

{{AUTH_KEYS_SALTS}}

$table_prefix = 'wp_';

define( 'WP_DEBUG', true );
define( 'WP_DEBUG_LOG', true );
define( 'WP_DEBUG_DISPLAY', false );

// Local-only marker so wp-light can detect and safely rewrite this file.
define( 'WPLIGHT_LOCAL_SITE', '{{SITE_NAME}}' );

if ( ! defined( 'ABSPATH' ) ) {
	define( 'ABSPATH', __DIR__ . '/' );
}

require_once ABSPATH . 'wp-settings.php';
